---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '5be65011-63cc-4577-a0f3-e5128d9823e8'
  PropagateID: '5be65011-63cc-4577-a0f3-e5128d9823e8'
  ReservedCode1: 'b03549f6-f9fc-43f3-975c-24f2519c1f63'
  ReservedCode2: 'b03549f6-f9fc-43f3-975c-24f2519c1f63'
---

# 智训通三窗口并行开发分工方案（零冲突）

> 前提：`admin-dashboard.tsx` 已完成拆分（commit `6080f89`），各待办区块已独立成组件文件。
> 三窗口在**不同文件**上并行开发，互不覆盖。

## 一、文件边界（核心原则）

| 窗口 | 负责模块 | 独立文件 | 禁止改动 |
|------|---------|---------|---------|
| **A** | P0 语音对练收尾 | `apps/admin/app/practice/page.tsx`、`apps/api/app/api/ai/tts/synthesize/route.ts` | 其他窗口的文件 |
| **B** | P1 系统管理 + P2 知识库 | `SysRolesSection.tsx`、`SysMenusSection.tsx`、`SysPostsSection.tsx`、`KnowledgeSection.tsx` | 其他窗口的文件 |
| **C** | P3 数据统计 + P4 申诉 | `StatisticsSection.tsx`、`AppealsSection.tsx` | 其他窗口的文件 |

**共享模块** `dashboard-shared.tsx`：原则上**只读**。若必须新增导出，需先在窗口间同步，避免重复定义同名导出。

## 二、当前已完成

- ✅ `admin-dashboard.tsx` 已拆分为 1 共享模块 + 6 独立组件（commit `6080f89`），编译通过、浏览器逐区块验收一致。
- ✅ 各区块已通过 props 接上主组件传入的数据（auth / records / organizations / navItems / pendingAppealCount / completedRecordCount 等）。
- ✅ 基线提交 `5901032`：任务/仪表盘统计已接真实后端数据。

## 三、各窗口待办清单

### 窗口 A — P0 语音对练收尾
- [ ] 验证对练中心语音链路（edge-tts 主引擎已就位，fallback 到本地 sherpa）
- [ ] 检查"点击说话/停止 AI 语音"交互 bug
- [ ] 语音模式切换、场景内固定音色复查
- **不触碰**：`StatisticsSection.tsx`、`Sys*.tsx`、`KnowledgeSection.tsx`、`AppealsSection.tsx`

### 窗口 B — P1 系统管理 + P2 知识库
- [ ] 角色/菜单/岗位三页接入真实后端 API（当前为静态数据）
  - 需新建 API：`apps/api/app/api/roles`、`/menus`、`/posts`；DB 需新增表（`packages/database/sqlite/init.mjs`）
- [ ] 企业知识库接真实 API（当前静态），支持文件夹/文件上传
- **注意**：改 `init.mjs` 加表时，用 `ensureColumn` 增量方式，勿动其他表已有逻辑

### 窗口 C — P3 数据统计 + P4 申诉
- [ ] 数据统计接真实数据（当前部门/学员两视图为静态）
  - 对接 `apps/api/app/api/dashboard/overview`（已含完成数/及格率/考试及格率）
- [ ] 申诉管理接真实数据（当前列表静态），补 `apps/api/app/api/appeals/[id]` 处理流
- **注意**：`AppealsSection` 的 `handleCreateAppeal`、`setAppealForm` 由主文件传入，改表单逻辑需同时改主文件 props 或改组件内部用本地 state

## 四、开发规范

1. **只 commit 自己窗口的文件**，`git add` 时明确到文件名，不 `git add .`。
2. **代码只读/拉取，严禁 push**，不建远程分支/tag/MR。
3. 中文 HTTP 测试用 Node/Python UTF-8 脚本（PowerShell 有编码黑洞），放 `.temp/`。
4. 改完跑 `npx tsc --noEmit -p apps/admin/tsconfig.json` 确认无 TS 错误，再在 `http://localhost:3000` 逐区块验证。
5. 浏览器验证不依赖截图盲猜；看不清就问宏哥。

## 五、环境速查

- 登录：`13800000000` / `Zxt@2026`，租户 `zxt-demo`；可用 `ALLOW_DEV_TENANT_HEADER=true` + 请求头 `x-tenant-code: zxt-demo` 免登录调 API（`http://127.0.0.1:4000`）
- 本机代理 `127.0.0.1:7897`，下载国外资源必须走代理
- 服务端口：ChatTTS(8179)/Whisper(8178)/API(4000)/Admin(3000)；重启用 `scripts/stop-local.mjs`、`start-local.mjs`

> AI生成
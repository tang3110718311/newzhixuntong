---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '38f4218d-4e2e-45be-8fd3-124bc3e944a6'
  PropagateID: '38f4218d-4e2e-45be-8fd3-124bc3e944a6'
  ReservedCode1: 'e64ad906-9590-493b-80a8-f7009d5e6fc5'
  ReservedCode2: 'e64ad906-9590-493b-80a8-f7009d5e6fc5'
---

# 智训通 zxt-next 通用 Agent 入口

部分 AI 工具识别 `AGENT.md`，部分工具识别 `AGENTS.md`。

本项目权威入口是 `AGENTS.md`。进入本工作目录后，请先读取并遵守：

1. `AGENTS.md`
2. `docs/AI协作/00-总控看板.md`
3. `docs/AI协作/01-项目当前状态.md`
4. 与当前任务匹配的 `docs/AI协作/tasks/Txxx-*.md`

本文件只做兼容桥接，不维护重复规则。

## 开发质量补充规则

1. 需求进入开发前，必须确认：需求来源、目标、做/不做边界、影响模块、角色权限、流程状态、验收标准。
2. 核心功能开发或修改时，必须覆盖正向、异常、逆向、边界四类场景；不能只实现主流程。
3. 改已有功能前，必须评估对历史逻辑、上下游接口、数据结构、权限控制、状态流转和测试范围的影响。
4. 涉及提交、审批、导入导出、批量任务、状态流转等高风险操作时，必须检查防重复提交、并发冲突、刷新回退、中断操作后的数据一致性。
5. 用户可见异常必须友好、准确、可引导；禁止暴露 SQL、路径、堆栈、服务内部信息；手机号、密码、证件号等敏感信息必须脱敏。
6. 涉及库表结构、批量改数、迁移、生产数据的操作，必须先说明风险、备份与回滚方案，得到确认后再执行。
7. 完成代码改动后，至少执行类型检查和与改动范围匹配的关键流程回归；结论要说明验证命令、验证结果和未覆盖风险。

> AI生成
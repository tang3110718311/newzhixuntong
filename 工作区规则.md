# 智训通 zxt-next 工作区规则

本文件给星辰超级智能体、WorkBuddy 等中文智能体作为工作区入口。

进入本项目后，不要只凭聊天上下文判断项目状态，请先读取：

1. `AGENTS.md`
2. `docs/AI协作/00-总控看板.md`
3. `docs/AI协作/01-项目当前状态.md`
4. 与当前任务匹配的 `docs/AI协作/tasks/Txxx-*.md`

所有项目铁律、任务边界、生产部署状态和未提交风险，以 `AGENTS.md` 与 `docs/AI协作/**` 为准；本文件只做中文入口，不重复维护规则正文。

